#!/usr/bin/env bash

set -e

cp "$(cd "$(echo "${BASH_SOURCE[0]%/*}")" && pwd)/../dist/docker-compose.yml" .

if [[ $1 != "" ]]; then
    sed -i \
        -e "s/7.3/$1/" \
        docker-compose.yml
fi
