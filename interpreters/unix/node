#!/usr/bin/env bash

set -e

if [[ ! "$(docker ps -q -f name=web)" ]]; then
    ant up
fi

USER=ant
if [[ $(whoami) == root || $(ant machine) == Windows ]]; then
    USER=root
fi

exec docker exec -u ${USER} web node "$@"
